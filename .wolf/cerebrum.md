# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-04-19

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->
- For release tags, prefer simple auto-resolved tags like `v20` from the git commit count instead of requiring manual semver tag input.
- Prompt rewrite output should favor concise, efficiency-directed, high-signal prompts for Claude Sonnet high effort instead of broad or verbose expansions.

## Key Learnings

- **Project:** terminal-64 — canvas-based terminal emulator + AI workstation built with Tauri v2 + React 19 + xterm.js. Manages multiple terminal sessions and Claude Code agents on a free-form pan/zoom canvas.
- Frontend provider-session implementation paths are provider-neutral: use `src/stores/providerSessionStore.ts`, `src/hooks/useProviderEvents.ts`, `src/components/provider-chat/*`, and `src/components/canvas/ProviderSessionDialog.tsx`. Claude-named paths are deprecated compatibility shims only.
- After the provider-neutral rename, active `ClaudeChat` / `useClaudeEvents` references under `src/` should be limited to compatibility shim exports in `src/components/claude/*` and `src/hooks/useClaudeEvents.ts`; use local names like `providerSessionStore` in shared helpers instead of `claudeStore`.
- Provider availability is user preference state, not a provider manifest concern: persist it in `settingsStore.providerAvailability`, filter only new-session/empty-chat picker options, and let saved or locked legacy sessions keep loading even when their provider is disabled.
- Provider session refresh must prefer persisted session metadata over mount-time defaults. `ProviderChat` may call `createSession` with `getDefaultAvailableProvider(...)` before history hydrates; `providerSessionStore.createSession` must let saved `providerState.provider`/flat `provider` win so OpenAI/Cursor sessions do not reload as Anthropic.
- Explicit provider session creation must persist synchronously when it includes a name, cwd, provider, or provider lock. Desktop restart can skip debounced creation saves before the first transcript-bearing mutation, leaving the canvas panel but no provider metadata row. Dialog-created provider sessions should lock the chosen provider; truly blank widget/empty sessions can remain unlocked.
- Provider session metadata cache must compare `localStorage.getItem(PROVIDER_SESSIONS_STORAGE_KEY)` with `lastPersistedMetaJson` before returning cached rows. Verification fixtures and migration/import paths can replace the storage key directly in the same renderer, and stale cache makes saved OpenAI rows reload as default Anthropic/unlocked.
- Terminal 64 skill integration is frontend-enforced for provider turns. `ProviderChat` keeps the displayed user text clean but sends a prompt augmented by `src/lib/skillPrompt.ts`, which lists available skills and auto-loads top matched `SKILL.md` bodies; backend `list_slash_commands(cwd)` must use the active chat cwd and mark entries with `kind` so commands are not treated as skills.
- Codex sessions should keep a bounded local transcript fallback in `runtimeMetadata.openai.runtimePayload.localTranscript` even though provider history is canonical. Fresh app-server threads can have a thread id before rollout JSONL is readable; reload should show the fallback transcript, and `codex_rollout_missing` should clear the stale thread id and seed the next fresh thread from visible messages. Transcript-bearing message mutations for Codex/Cursor should save synchronously because desktop reload/quit can skip idle debounced saves.
- Codex/Cursor compact metadata saves must not treat local transcripts like fork seed transcripts. `saveToStorage` quota retry may drop fork seed transcripts, but must preserve a smaller bounded `runtimePayload.localTranscript` for active OpenAI/Cursor sessions; otherwise fresh Codex sessions can reload empty while old rollout-backed chats still survive.
- Provider metadata quota handling must guarantee active session rows survive even when preserving old metadata is impossible. After full and compact saves fail, write active-session-only metadata with a tiny transcript cap, then active metadata without local transcripts as a last resort; a visible named/provider-selected panel is more important than retaining inactive metadata rows.
- Provider metadata forward-compat must be row-scoped. Seeing one future `schemaVersion` row should preserve that row but continue saving new/current rows; a global read-only lock makes old chats survive while all brand-new chats silently fail to persist.

### Windows platform
- No first-class `longPathAware` manifest field in Tauri 2; keep `~/.terminal64/` paths short.
- Localhost servers (127.0.0.1) don't trigger Windows Defender Firewall prompts. SmartScreen needs EV cert (out of scope).
- Prompt rewrite uses the host-visible Claude CLI via `rewrite_prompt` -> `resolve_claude_path()`/`shim_command()`. On native Windows, users need native Claude Code installed/authenticated; a WSL-only `claude` install is not visible to the Windows Tauri app unless explicitly bridged.

### Voice / ONNX runtime (added 2026-04-18)
- Moonshine-base: encoder hidden dim **416** (not 288); decoder 8 layers (64 past_kv inputs) + `use_cache_branch: bool[1]`. For ≤3s audio, pass `use_cache_branch=false` + zero past tensors each step.
- Silero-VAD v5 ONNX: unified `state: [2,1,128]` (not separate h/c). Inputs: `input`, `state`, `sr` (i64[1] not i64[]). Outputs: `output`, `stateN`.
- openWakeWord: wake-classifier input is `x.1`. Always resolve names at load via `session.inputs()[0].name()` — naming has changed across versions.
- openWakeWord melspectrogram.onnx output is `[time,1,1,32]` (4D, batch on axis 0). Flatten by total-len/32; apply `x/10 + 2` scaling.
- LocalAgreement-2 word-index LCP only valid for SAME audio region. When partial decoder slides its window, fully reset committed_words + last_hypothesis_words. Without per-word timestamps you can't preserve committed prefix across slides; final beam-search decode backfills.
- Tauri Emitter split-stream events (`voice-committed` + `voice-tentative`) arrive in non-deterministic order. Each handler updates its half of the store and calls a shared `applySplit()` reader — don't await pairs.

### Delegation
- `delegationStore.parentToGroup` is single-slot. To get ALL groups for a parent, iterate `Object.values(delState.groups).filter(g => g.parentSessionId === parentId)`.
- Delegation children are provider-bound but ephemeral. When spawning them, explicitly copy parent session model/effort and Codex permission preset into both the child store row and backend create request; `createSession(..., ephemeral=true)` does not load persisted metadata.
- Claude delegation MCP labels come from the generated temp MCP config env. Create one config per child if the team chat needs distinct `Agent N` names.
- Delegation child provider prep lives in `src/lib/delegationChildRuntime.ts`: resolve inherited provider/model/effort/Codex permission there, build Claude MCP config vs Codex MCP env there, and keep `useDelegationSpawn.ts` focused on store/session orchestration.
- Delegation child runtime permissions are manifest-owned: `delegation.childRuntime.permissionPreset === "selected"` must pass the selected `providerPermissionId` and omit `permissionOverride`; `"bypass_all"` may set `permissionOverride: "bypass_all"` plus a provider bypass permission id. Do not hard-code bypass for every delegated child.
- Delegation completion parsing lives in `src/lib/delegationCompletion.ts`: normalize provider/MCP-prefixed `report_done` tool names/results there, return a `report_done` vs `idle_candidate` decision, and consume provider lifecycle through `src/lib/providerLifecycleBus.ts` instead of direct Claude hook listeners.
- Provider lifecycle signals for delegation now flow through `src/lib/providerLifecycleBus.ts`: Claude `SubagentStart`/`SubagentStop` hooks normalize to agent-harness events, while provider turn start/complete transitions normalize to shared turn lifecycle events for current and future providers.

### Claude CLI events (`useClaudeEvents.ts`)
- `stream_request_start` fires at start of EACH API call in a multi-turn session. Treat like `message_start`: clear pendingBlocks + assistantFinalized.

## Do-Not-Repeat

<!-- Past mistakes that must not recur. Each entry dated. -->

### Windows shim / PATH (2026-04-19)
- `Command::new("pm2"|"claude"|"openwolf")` does NOT resolve `.cmd`/`.bat` shims via PATHEXT. Invoke via `cmd /C <shim>` with CREATE_NO_WINDOW (0x08000000). Use centralized `shim_command` helpers (`claude_manager::shim_command`, `lib.rs::shim_command`) — do not open-code at call sites.
- `where <bin>.exe` only matches exact extension; pass bare name to use PATHEXT. Same for `Command::new` — fallback strings must include `.cmd`/`.exe`.
- Provider CLI cancel/close on Windows must kill the whole process tree, not just the direct `cmd /C` wrapper. Use `providers::util::terminate_child_process` for Claude/Codex/Cursor children so `taskkill /T /F` reaches the real Node/provider process.
- Provider subprocess PATH is intentionally expanded in `src-tauri/src/providers/util.rs::expanded_tool_path` because macOS/Windows GUI apps do not inherit login-shell PATH. Add common language/toolchain bins there instead of one-off per-provider env hacks; Roblox linters installed by Aftman/Rokit/Foreman need `~/.aftman/bin`, `~/.rokit/bin`, and `~/.foreman/bin` available to Codex/Claude/Cursor child commands.

### Windows filesystem (2026-04-19)
- `std::os::windows::fs::symlink_dir` requires Admin/Developer Mode. Fall back to junctions via `cmd /C mklink /J` (see `create_dir_link()` in lib.rs).
- NTFS reserves `CON`, `PRN`, `AUX`, `NUL`, `COM1-9`, `LPT1-9` regardless of extension; strips trailing dots/spaces. Sanitize external filenames (Discord attachments, uploads).
- Frontend: use `src/lib/platform.ts` helpers (`baseName`, `dirName`, `isAbsolutePath`, `joinPath`, `IS_WIN`, `IS_MAC`). Do NOT use `path.split("/")`, `relPath.startsWith("/")`, template-literal joins, or `navigator.platform` directly — they all fail on Windows backslash/drive-letter paths.

### Tauri build config (2026-04-19)
- Tauri 2 bundle: use explicit `targets: ["app","dmg","deb","appimage","rpm","nsis"]` — `"all"` triggers MSI which needs WiX. Set `bundle.windows.nsis.minimumWebview2Version` (e.g. "110.0.1587.40") for enterprise updates.
- `env!("CARGO_MANIFEST_DIR")` bakes developer's compile-time path — production crashes on user machines. Use `app_handle.path().resource_dir()` first, fall back to CARGO_MANIFEST_DIR only for dev runs.
- Rust CI runs clippy with `-D warnings`; prefer direct cheap fallbacks like `unwrap_or(JsonValue::Null)` over lazy `unwrap_or_else(|| json!(null))`.
- macOS dev linking can fail after CLT/Xcode updates when Rust retains a stale Clang runtime search path (`ld: library 'clang_rt.osx' not found`). `src-tauri/build.rs` should query the active compiler resource dir and add `<resource>/lib/darwin` when `libclang_rt.osx.a` exists, rather than hard-coding a CLT version.

### ONNX / ort crate (2026-04-18)
- `ort` 2.0.0-rc.11 needs `ndarray = "0.17"` (matches fastembed v5 transitive). Pinning 0.16 splits the graph and breaks `Tensor::from_array`.
- `ort::Session::run(...)` returns `SessionOutputs<'s>` borrowing the session — scope each stage in `let x = { ... }` so outputs drop before `&mut self` calls (E0499). Applies to all voice runners.

### Rewind / git / delegation data loss (2026-04-19)
- Rewind `restoreCheckpoint(keepTurns+1)` restores PREVIOUS turn's snapshot. Detect undo-send (target = last user msg, no assistant response) and skip file ops to prevent data loss.
- Rewind MUST NOT `git checkout HEAD -- path` on delegation-modified files — wipes uncommitted/parent edits. For files touched by delegation children without parent checkpoint entry, only delete UNTRACKED files.
- Distinguish `Err(spawn failed)` from `Ok(exit != 0)` for git. Spawn fail = skip-and-log; non-zero exit = safe to delete untracked. Treating spawn fail as "untracked" causes data loss on tracked files.

### Session state flow (2026-04-16)
- Fork first-send MUST branch on `forkParentSessionId` presence alone — NOT `if (forkParent && !started)`. `loadFromDisk(newSessionId, forkedMessages)` sets `hasBeenStarted=true` whenever `promptCount>0`, so the `!started` guard falls through to `--resume <newId>` against a JSONL the CLI hasn't created yet, and the send hangs silently. Store clears `forkParentSessionId` after the `--fork-session` send succeeds.
- With `exactOptionalPropertyTypes`, provider runtime input objects must omit optional fields when values are undefined. Do not pass `permissionOverride: undefined` or `codexThreadId: undefined`; conditionally add the property.
- Backend runtime managers should remove entries on both explicit close and natural process EOF. PTY readers need generation checks before removing map entries, and Claude session ids must be generated before permission-server registration so temp hook/MCP files are keyed to the id the frontend later closes.
- Provider fork panels must seed the child `claudeStore` row with the parent provider synchronously before any async fork prep. Adding a canvas panel first and awaiting before `createSession(..., provider)` lets `ClaudeChat` mount and create the child as default Anthropic, which makes Codex forks open as broken Claude sessions.
- Provider-state migrations must keep runtime fallback to legacy flat session fields (`provider`, `codexThreadId`, `selectedCodexPermission`) for hot-reloaded in-memory sessions. TypeScript sees new fields as required, but live Zustand state can predate the migration.

### UI rendering / WebKit (2026-04-18)
- `overflow: hidden` + `border-radius` on a child of a `transform: scale(z)` parent flickers on WebKit at fractional pixel positions — rounded-corner clip repaints non-atomically, invalidating the background paint. Fix by promoting to own compositor layer: `will-change: transform; transform: translateZ(0); backface-visibility: hidden; contain: layout paint; isolation: isolate`.
- Prompt island can lag with huge histories if it reverses/maps/formats every prompt on open. Render the newest bounded slice first and append older rows in scroll batches.
- LegendList scroll events are not a reliable proxy for user intent during streaming; growing rows can briefly report not-at-end. Keep bottom stickiness in an intent ref updated by wheel/touch/key/pointer handlers, and use scroll events only for visibility/progress telemetry.
- Big-workflow performance: avoid per-panel global Tauri listeners and idle RAF polling. Terminal output should go through a single keyed dispatcher, browser panel bounds should sync from canvas/resize events, and full JSONL hydration caches must be capped by entry/message count.
- Big-workflow renderer hot paths: window-level Tauri drag/drop should be routed through one shared listener for all chat panels, and canvas-store subscribers must guard on the exact fields they consume because vanilla Zustand subscribers fire on every store write.
- Frontend persistence hot paths: localStorage saves should be dirty-triggered and deduped by serialized payload, not polling-based; large metadata stores should keep an in-memory parsed cache, schedule writes through idle time, and prune/compact stale inactive rows before retrying quota failures.
- 5-second lag spikes can come from renderer-main-thread persistence, not rendering alone. On 2026-04-27 the active WebKit localStorage had `terminal64-delegations` around 1.2MB and 18 restored panels; avoid permanent save intervals and dedupe/idle-schedule any JSON.stringify + localStorage writes.
- Widget/browser panels: hot-reload polling should baseline the first mtime, skip while `document.hidden`, and prevent overlapping `widget_file_modified` IPCs. Embedded native browsers should use event-coalesced canvas/resize bounds sync rather than idle RAF loops, and async native webview creates must close the webview if the React panel unmounts before the create promise resolves.
- Widget memory diagnostics cannot reliably read exact per-iframe heap from the host. Track low-overhead host-side signals instead: batched bridge traffic, reload/load churn, retained plugin/bus/voice/terminal subscriptions, embedded browser state, renderer-wide `performance.memory` when available, and optional iframe self-reports via `t64:debug-metrics`.
- Widget bridge live AI-session events must be opt-in. Default-pushing `streaming-text`/message/tool/session events to every iframe creates a renderer-wide postMessage firehose; widgets should use `t64:request-state`/`t64:request-messages` for snapshots and `t64:subscribe-session-events` only when they really need live events. Throttle `streaming-text` even after opt-in.
- Bundled widgets should not run fixed short polling loops for status data. `project-intel` daemon status polling at 5000ms lined up with visible lag spikes; use non-overlapping, visibility-aware, slower polling and event/broadcast refreshes for immediate updates.
- Canvas session persistence must be quiet-time debounced and idle-scheduled, not a fixed throttle. A 5000ms throttle can create periodic main-thread localStorage spikes while a panel remains dirty; log slow `saveSession` calls with byte count so runtime reports identify persistence stalls.
- Renderer lag debugging now has a global monitor (`usePerformanceMonitor` + `performanceStore`): record event-loop heartbeat drift, Long Task API entries when supported, and slow Storage writes. Settings exposes a Performance section and console emits `[perf]` warnings; use those events before further performance guesses.
- If a user-visible hitch records zero event-loop/long-task/storage events, add frame-drop monitoring and isolation switches. Compositor/GPU/iframe work can drop frames without a long JS task; `requestAnimationFrame` gaps plus a Settings `widgetsPaused` toggle help separate app-render jank from widget iframe jank.
- Widget jank isolation should support both global and per-widget iframe pausing. Once global `widgetsPaused` proves the hitch is widget-side, use persisted `pausedWidgetIds` and per-card Pause/Resume controls to binary-search the exact widget without deleting panels.
- Widget-owned WebSocket tails can jank the whole renderer even when Terminal 64 bridge counters are low. The installed `ro-sync` widget's active view appended a DOM row and forced scroll for every daemon op, then still lagged because every ignored op frame was JSON-parsed first. Batch iframe log rendering with RAF, cap pending rows, preserve user scroll position, and add a raw-frame skip/sampling hook before JSON.parse for high-volume op streams.
- Long-term widget performance isolation should not rely on iframe sandboxing. Keep iframe as a compatibility transport, but add a native Tauri child-webview render mode for heavy widgets and move the widget bridge behind a transport-agnostic host broker. Web Workers help stream parsing/compute but cannot replace visual widget DOM.
- Widget bridge host extraction lives in `src/components/widget/useWidgetBridgeHost.ts`: it owns command handling, widget resources, provider-turn helpers, and opt-in session event fanout; `WidgetPanel.tsx` should only wire iframe lifecycle/postMessage transport and panel-local browser bounds/hot-reload behavior. Future native widget transports should call the host's `handleRequest()`/`sendInit()` path instead of duplicating the switch.
- Widget bridge `t64:create-session` has three provider-lock paths: explicit valid `provider` locks to that provider; prompt without provider resolves the active provider session (if any) or Anthropic fallback and locks before sending; no prompt and no provider creates an unlocked empty provider chat so the picker can choose the first real turn.
- Fresh WidgetDialog-created widgets should call `writeWidgetInstructionFiles(widgetId)` after `createWidgetFolder`, then open the widget panel plus a blank provider chat with `providerLocked=false`; do not seed the chat with widget-building prompt text.
- Native widget bridge broker contract: native webviews use `POST /widgets/{widgetId}/bridge/{instanceId}/request` for widget-originated messages and `GET /widgets/{widgetId}/bridge/{instanceId}/events` for SSE host events. Rust emits `widget-bridge-request` to the main webview, and the frontend resolves/fans out through `widget_bridge_respond` and `widget_bridge_emit_event`. Keep iframe `postMessage` as the compatibility transport.
- Widget native webview IPC is intentionally separate from embedded browser IPC. Backend labels are namespaced as `widget-webview-{id}` while frontend callers use stable widget instance ids; keep iframe rendering as the default compatibility path until a render-mode switch opts into native.
- Widget render-mode state lives in `settingsStore` as a global `widgetRenderMode` plus per-widget `widgetRenderModesById` overrides. Until native widget transport is fully wired, `resolveWidgetRenderMode()` must keep `native-webview` and `auto` requests effective as `iframe` so existing widgets keep loading.
- Widget host protection policy lives in `src/lib/widgetHostProtection.ts`: default mode is observe-only, `Auto Pause` adds the selected widget id to persisted pause settings after 4 visible frame drops in 15s, and `Auto Promote` requests per-widget `native-webview` render mode while `resolveWidgetRenderMode()` keeps iframe fallback until native transport is ready. Keep known noisy defaults such as `ro-sync` in that module so frame-drop diagnostics and Settings use the same source of truth.
- Fresh widget creation should write provider-readable `CLAUDE.md` and `AGENTS.md` files into the new `~/.terminal64/widgets/{id}/` folder, then open a blank provider-unlocked chat pointed at that folder. Existing/reopened widget sessions and widget bridge sessions with provider/prompt provenance remain provider-locked; verification notes live in `docs/provider-picker-verification.md`.
- Settings diagnostics controls should stay grouped at the bottom under a collapsed Debug section so normal preferences remain clean. If a widget view auto-starts a daemon stream, guard against duplicate WebSocket opens on repeated state broadcasts and close the stream on daemon-down events.
- CI uses floating stable Rust; on 2026-04-26 GitHub Actions picked Rust 1.95.0 while local was 1.94.1. When CI Clippy fails but local passes, run `rustup toolchain install stable --component rustfmt --component clippy --profile minimal --no-self-update` and verify with `cargo +stable clippy --all-targets -- -D warnings`.
- Rust stability tests in production modules should put `#[cfg(test)] mod tests` after all non-test items. CI runs Clippy with `items_after_test_module` denied through `-D warnings`.
- Tauri drag/drop events are delivered at the webview/window level, not scoped to individual chat panels. Route chat file drops by `payload.position` (physical pixels; divide by `window.devicePixelRatio`) to the hit `.cc-container[data-session-id]`, then nearest visible chat rect. Do not route by last active chat.

### Voice pipeline (2026-04-18)
- VAD with a single hard threshold + 1-frame hangover finalizes whisper early on breaths/soft consonants mid-utterance. Use Silero VADIterator-style hysteresis: activate 0.5, deactivate 0.35, `min_speech_duration_ms=250`, `min_silence_duration_ms=500`, `speech_pad_ms=300`. Tune orchestrator silence-frame counters to match (e.g. dictation `SILENCE_FRAMES_TO_FINALIZE` 25→9).
- Streaming `partial_worker` that clones the full rolling buffer every tick is O(n²) over an utterance — 12s audio re-decoded every 250ms stalls partials under Metal. Decode only a trailing window slice (`g[buf_len - PARTIAL_WINDOW_SECS*SR ..]`), add min-new-samples gate (~120ms), reset `AgreementBuffer` when the window slides, and watchdog-skip next tick if decode > 2× tick interval.

### Security / zip extraction (2026-04-19)
- Zip extraction: `PathBuf::starts_with` is lexical — does NOT collapse `..`. Iterate `components()` and reject `ParentDir`, `RootDir`, `Prefix` before joining (Windows zip-slip).

### Session JSONL handling (2026-04-19)
- `find_rewind_uuid` leaf detection must filter to `type == "user" | "assistant"`. JSONL also contains `summary`, `task-summary`, `mode-entry`, etc. with UUIDs that aren't conversation tail.
- Claude JSONL can thread user/assistant records through `attachment` records and can append Stop-hook attachments after an assistant. Leaf detection must ignore attachments as transcript children, but parent-chain traversal must still retain all UUID records so it can walk through attachments.
- `truncate_session_jsonl_by_messages` trailing sweep: on JSON parse failure, `continue` (don't `break`). A single malformed line from in-flight CLI writes must not abort the sweep and break tool_use/tool_result pairing.

### Rewind + fork JSONL persistence (2026-04-22)
- `load_session_history` reads every line in the JSONL sequentially — it does NOT walk the parentUuid chain and does NOT respect rewind markers. Rewind must physically truncate the file (`truncate_session_jsonl_by_messages`); marker-only rewinds reload the "deleted" messages on refresh.
- Claude rewind after physical JSONL truncation should use normal session resume and clear `resumeAtUuid`. Passing `--resume-session-at` after rewriting the file can make the CLI reject a kept UUID with "No message found with message.uuid", leaving the chat unable to continue.
- `fork_session_jsonl` copies the full parent JSONL (needed so chain-walking can resolve the fork UUID) but MUST then truncate the destination to `keep_messages`. Without truncation the fork reloads with the entire parent history until the first `--resume-session-at` send.
- Codex app-server rewind differs from Claude: it may preserve older rollout items and append a `thread_rolled_back`/`thread_rollback` marker with `num_turns`/`numTurns`. Codex history hydration must replay that marker by dropping trailing user turns; otherwise refresh-from-jsonl shows messages the model context no longer contains.
- Codex rollouts can store edits as `custom_tool_call` named `apply_patch`; hydrate these into `Edit`/`MultiEdit` tool calls so refreshed chats preserve Claude-style clickable edit cards.
- Codex file-change shapes are not stable across live app-server events and rollout/history data: accept `path`, `file_path`, `filePath`, `diff`, `unified_diff`, and `unifiedDiff` when building UI tool-call inputs.
- Tool edit preview paths can be relative for Codex `apply_patch` history; resolve them against the session cwd before `readFile` so Monaco opens the real file.
- Checkpoint snapshots must also resolve Codex/Claude modified file paths against the session cwd before `readFile`/manifest persistence; restore must only write absolute manifest targets. Raw relative paths would restore from the Tauri process cwd, not the project cwd.
- Legacy `T64_CODEX_TRANSPORT=exec` emits rollout-style `session_meta`/`event_msg`/`response_item` JSON. Translate it in Rust to the same provider-neutral event shapes as app-server before emitting to the frontend.
- Codex app-server/live item updates may use cumulative `item.text`/`output` or delta fields depending on event source. Frontend accumulation must append explicit `delta` values but replace when the new value already includes the previous value; patch-only file-change updates should update tool input without setting `result`, or the UI marks the tool completed too early.
- Codex rollout/history tool names are raw model/tool ids (`exec_command`, `write_stdin`, `apply_patch`, etc.) while live app-server events may already be semantic (`commandExecution`, `fileChange`). JSONL hydration must normalize raw shell tool names into the same `Bash` card shape as live events, or refresh-from-jsonl renders a totally different chat.
- Codex runtime has two ids: the Terminal 64 local `sessionId` routes UI/process events, while `codexThreadId` is the external OpenAI thread used for resume/fork/rollback. Fresh first-turn failures must not fall back to `send_codex_prompt` without a thread id; only already-started legacy sessions may attempt local-id resume.
- With `exactOptionalPropertyTypes`, provider contract fields that callers include explicitly with maybe-undefined values must be typed as `T | undefined`, not just `field?: T`.
- Backend provider adapters now have two layers: the future normalized async `ProviderAdapter` trait, plus a narrow synchronous `ProviderCommandAdapter` used by existing Tauri IPC wrappers. Route common create/send/cancel/close commands through `ProviderRegistry` while preserving provider-specific request structs and frontend command names.
- Backend provider create/send command adapters receive `ProviderCommandRequest { payload, context }`; concrete adapters own serde deserialization of provider-specific payloads. Keep central `provider_create`/`provider_send` limited to provider-id routing and cross-cutting setup such as Claude permission-server sidecars and OpenWolf cwd hooks, not provider-specific field enums.
- Generic provider IPC uses frontend provider ids (`anthropic`, `openai`) at the Tauri boundary (`provider_create`, `provider_send`, `provider_cancel`, `provider_close`) and maps them to Rust `ProviderKind`; Claude/Codex compatibility command names should remain thin wrappers over that generic path.
- Frontend provider IPC request contracts live in `src/contracts/providerIpc.ts` as generic `ProviderIpcRequestEnvelope` plus augmentable provider request maps. Provider runtimes register their own create/send/history payload shapes through module augmentation; future providers should add a runtime-owned map entry instead of editing central Anthropic/OpenAI unions.
- Remaining provider-addition blockers after the IPC/history refactors are mostly: legacy event ingestion (`claude-event`/`codex-event` in `useClaudeEvents.ts`), OpenAI-specific fields in shared `ProviderSessionState`, two-provider permission cycling in `ProviderControls`/`ClaudeChat`, provider-specific delegation transport config in `delegationChildRuntime.ts`, and `spawnProviderSessionWithPrompt` still branching instead of using `runProviderTurn`.
- Codex plan mode in app-server is a real collaboration mode, not just `/plan` text. Send `turn/start` with `collaborationMode: { mode: "plan", settings: { model, reasoning_effort, developer_instructions: null } }`; `developer_instructions: null` selects Codex's built-in Plan instructions. Plan content streams as `item/plan/delta` and completed `plan` items generated from `<proposed_plan>` blocks. `update_plan` remains the TODO/checklist tool and is explicitly distinct from Plan mode.
- Codex app-server MCP startup status uses `mcpServer/startupStatus/updated` with `{ name, status, error }`; `status: "ready"` means connected. Terminal 64 should translate this into the live MCP store and merge it over configured entries instead of replacing the whole dropdown list.
- Codex app-server 0.125 generated contracts require `experimentalRawEvents` and `persistExtendedHistory` on `thread/start` and `thread/resume`, and text inputs for `turn/start` need `text_elements: []`. Also mark sessions streaming before provider events arrive; otherwise rapid sends can kill/replace the app-server worker before `turn/start`.
- Codex app-server lifecycle should reject overlapping local-session turns instead of killing the existing worker. Keep legacy `T64_CODEX_TRANSPORT=exec` fallback behavior separate; for app-server, surface `initialize`/`thread/start|resume`/`turn/start` failures with method-specific errors, startup timeouts, and initialize server/protocol diagnostics.
- Codex fork fallback must stay under app-server's turn/start input cap. Do not prepend an unbounded visible transcript or raw full tool input JSON; render recent messages latest-first with per-message/tool caps and a hard prompt budget below the protocol limit.
- Upstream Codex app-server supports native `thread/fork` by `threadId` and by rollout `path`; `ThreadForkParams` also requires `persistExtendedHistory`. Terminal 64 should try native `thread/fork` first, retry with the discovered `~/.codex/sessions/.../rollout-*.jsonl` path if thread-id lookup fails, and use seeded transcript only as a last-resort fallback.
- Claude JSONL hydration is read-only and should be more forgiving than write/truncate paths: if `<stored-cwd-hash>/<sessionId>.jsonl` is missing, scan `~/.claude/projects/*/<sessionId>.jsonl` before returning empty because the CLI can normalize cwd differently while still resuming by session id.
- When `createSession` adopts a cwd for an existing non-ephemeral session row, persist metadata immediately. Otherwise a session can keep working in the current process but reopen without the cwd needed for JSONL hydration.
- Claude `--session-id <id>` must only be used for truly new transcripts. If a recovered/empty-looking UI session already has `~/.claude/projects/*/<id>.jsonl`, spawn with `--resume <id>` up front because the CLI reports "Session ID ... is already in use" asynchronously after providerCreate returns, too late for frontend create->send fallback.
- ClaudeChat cleanup direction: move provider/UI slices into small components/hooks first, while leaving the main chat shell intact. `McpMenu.tsx` now owns configured/live MCP merging and T64 alias dedupe so new providers do not touch topbar dropdown internals.
- ClaudeChat's main `session` selector intentionally omits `streamingText` to avoid per-token rerenders. Extracted hooks/components that receive `session` should type only the fields they actually read instead of requiring the full `ClaudeSession`.
- ClaudeChat message list cleanup: `useChatRows.ts` owns virtual row descriptor construction and prompt index data, while `ChatMessageList.tsx` owns LegendList rendering. Keep layout-sensitive row keys in the hook and sticky-scroll intent refs in the shell unless the scroll state itself is extracted.
- ClaudeChat edit overlay cleanup: `ChatEditOverlay.tsx` owns Monaco rendering, dirty/save state, changed-line decorations, cached edit contents, and scroll restoration through `useChatEditOverlay`. Keep file IO provider-neutral by passing `readFileContent`/`saveFileContent` callbacks from the chat shell.
- `ProviderControls.tsx` owns the chat topbar MCP/model/effort dropdown rendering/open-state and provider-specific permission status prop derivation for `ChatInput`; keep provider runtime create/send/cancel wiring in `ClaudeChat.tsx`.
- Provider manifests live in `src/lib/providers.ts`: use `getProviderManifest()` / `providerSupports()` for UI labels, model/effort/permission options, and topbar/input capability gates. `PROVIDER_CONFIG` remains as a compatibility alias.
- Provider permission controls are manifest-driven: `ProviderManifest.permissionControl` declares whether the active permission comes from Claude settings or provider metadata, `src/lib/providerPermissions.ts` formats/cycles manifest options, and `useProviderPermissionControls()` keeps Claude modes plus Codex sandbox presets out of `ClaudeChat`/`ProviderControls`.
- Provider manifests also gate native slash-command and compact support. Keep Claude built-in slash commands, `/compact` dividers, auto-compact hints, and Compact & Build controls behind `providerSupports(provider, "nativeSlashCommands" | "compact")`; Codex should still list skills/project commands but not Claude built-ins.
- Provider event/tool decoding boundary lives in `src/contracts/providerEvents.ts` plus `src/lib/{claude,codex}EventDecoder.ts`: raw provider IPC field drift should be normalized into `ProviderToolCall`/`ProviderToolResult` and helper-read file/change fields before store or `ChatMessage` code sees it.
- Provider live-event ingestion now lives in `src/lib/providerEventIngestion.ts`: it owns raw `claude-event`/`codex-event` and done subscriptions, routes through provider-owned decoder classes, and emits normalized provider events for `useClaudeEvents.ts`; keep future raw provider streams out of React hooks.
- Provider event side effects now flow through `src/lib/providerEventSemantics.ts`: derive MCP status, hidden tool visibility, plan/task state, pending questions, modified files, and delegation requests from `NormalizedProviderEvent`, then let `useProviderEvents` apply provider-neutral store/UI handlers. Keep StartDelegation parsing single-sourced in `src/lib/delegationWorkflow.ts`.
- Provider turn finalization must read a fresh Zustand snapshot after `flushBeforeFinalization()`. Fast providers like Cursor can emit assistant text and `turn_completed` in the same tick, so reading from the pre-flush state object drops the just-flushed streaming text.
- Backend raw provider events now emit a unified `provider-event` envelope (`{ provider, sessionId, data }`) before legacy `claude-event`/`codex-event`; frontend ingestion subscribes to unified first and uses legacy channels only until a provider proves unified support.
- Provider session metadata is canonical in `ProviderSession.providerState`; flat `provider`, `codexThreadId`, `selectedModel`, `selectedEffort`, and `seedTranscript` are compatibility mirrors maintained by `providerSessionStore.updateSession`. OpenAI permission ids are not live flat mirrors; `selectedCodexPermission` is only a legacy persisted-state read/migration shim into `providerState.providerPermissions.openai`. Provider-owned details live in `providerState.providerMetadata[providerId]` (for Codex/OpenAI, `getOpenAiProviderSessionMetadata()` is thread metadata only).
- Empty user-created provider chats are the only intentionally provider-unlocked sessions. Reopened/saved/disk, forked, delegation child, widget, skill, and seeded prompt sessions must pass an explicit lock signal at creation/reopen so the empty-state provider picker cannot retarget provider-owned history.
- Empty-chat provider switching must go through `switchProviderBeforeStart(sessionId, provider)`. The action refuses locked/started/history-backed/runtime-backed sessions and resets provider defaults; first user turns and disk hydration preserve the lock in both `providerState.providerLocked` and the flat compatibility mirror.
- Frontend consumers should use `resolveSessionProviderState()` / `selectSessionProvider()` for routing and `getProviderSessionMetadata()` / `selectSessionProviderMetadata()` for provider-owned metadata. Direct reads of flat `ClaudeSession` mirror fields should stay inside `claudeStore` migration/backcompat code.
- Frontend provider runtime operations live behind `src/lib/providerRuntime.ts`: route create/send/cancel/close/rewind/fork/hydrate through `src/lib/providerRuntimes/{anthropic,openai}.ts` instead of adding provider branches to hooks or store hydration.
- Provider history IPC now has generic commands (`provider_history_truncate`, `provider_history_fork`, `provider_history_hydrate`) keyed by frontend provider ids. Runtime adapters should call these wrappers; legacy Claude JSONL/Codex rollback/fork/hydrate command names are compatibility shims only.
- Provider history support is explicit at both runtime and backend adapter boundaries. Frontend `ProviderRuntime.history` owns `capabilities` plus optional hydrate/fork/rewind/delete handlers; generic helpers return structured `unsupported` results when a future provider has no handler. Backend `ProviderAdapterCapabilities.history` drives `provider_history_*` fail-closed responses for known but unbound providers, while Claude JSONL and Codex rollout/app-server handlers return `applied`/`skipped`/`unsupported` status payloads.
- Provider transcript ownership is now explicit as manifest/runtime `history.source`: Anthropic uses `claude-jsonl`, OpenAI/Codex uses `codex-rollout`, Cursor uses `local-transcript`, and future unsupported stubs use `none`. `claudeStore` should key local transcript persistence and hydrate labels off this source, not provider id checks.
- Backend generic `provider_history_*` commands should only resolve `ProviderKind` and delegate through `ProviderRegistry`; provider adapters own history request parsing/result shaping. Keep Claude JSONL and Codex rollout/app-server details behind adapter methods, with registry capability checks returning structured `unsupported` before adapter calls.
- Explicit chat refresh needs a separate authoritative history path: normal startup hydration should stay extend-only to avoid clobbering live in-memory turns, while user-triggered refresh may replace/shrink messages only when provider history returns a non-empty transcript.
- Provider architecture docs should describe the two-layer design: frontend provider manifests/runtimes route Anthropic/OpenAI operations, backend generic `provider_*` IPC dispatches through `ProviderRegistry`, and Claude/Codex-specific command names are compatibility wrappers.
- Provider-neutral UI/workflow surfaces should call `runProviderTurn` with resolved `providerState` and apply `ProviderTurnResult` cleanup flags; provider-specific request details such as Claude `disallowed_tools` belong on `ProviderTurnInput` and are mapped inside the provider runtime.
- `src/lib/tauriApi.ts` must lazy-import `runProviderTurn` from `src/lib/providerRuntime.ts` for helper paths like `spawnProviderSessionWithPrompt`; a static import creates a runtime cycle because provider runtimes import `tauriApi` for the generic IPC wrappers.
- `spawnProviderSessionWithPrompt` first-turn inputs should be built from provider manifest defaults (`listProviderControls`, `getDefaultProviderPermissionId`) and only include Claude-shaped `permissionMode` for Anthropic. OpenAI/Cursor seeded prompts should pass `providerPermissionId` and let provider runtimes map provider-owned permissions; omit optional fields like `skipOpenwolf` when undefined.
- `claudeStore.promptQueue` entries are rich provider-turn payloads, not display strings: use `queuedPromptDisplayText()` for UI/history, `queuedPromptProviderPrompt()` for the raw provider prompt, and preserve permission override / Codex collaboration mode / command metadata when enqueueing.
- Prompt queueing must happen after command-specific provider prompts are built but before `actualSend`; special commands like `/delegate`, skill slash commands, `/reload-plugins`, and `/loop` need queued command metadata so replay can restore side effects and avoid sending into an active provider turn.
- Provider-neutral frontend helper cleanup can wrap legacy Claude-shaped surfaces instead of removing them: keep compatibility exports like `spawnClaudeWithPrompt` and `closeClaudeSession`, but route new UI/helper callers through provider-neutral names such as `spawnProviderSessionWithPrompt` and `closeProviderSession`.
- Provider-neutral frontend shell/store cleanup follows the same wrapper pattern: prefer `ProviderChat`, `ProviderSessionDialog`, `useProviderEvents`, `useProviderSessionStore`, `PROVIDER_SESSIONS_STORAGE_KEY`, `ProviderSession`, `ProviderTask`, and `ProviderSessionStoreState` for new code while keeping the legacy Claude-named exports as deprecated compatibility aliases.
- Provider-neutral frontend module paths can be introduced with thin re-export entrypoints (`providerSessionStore`, `useProviderEvents`, `ProviderChat`, `ProviderSessionDialog`, `providerTypes`) before physically moving legacy Claude-named implementation files; this avoids destabilizing parallel work while giving new callers neutral imports.
- Provider controls are now generic manifest controls: read `listProviderControls(provider, placement)` and `selectedControls[control.id]`; `selectedModel`/`selectedEffort` and legacy slots are compatibility mirrors only. Cursor's agent mode is the `mode` control and apply behavior is the `apply-mode` permission/input control.
- Provider manifest raw definitions live as `ProviderManifestDefinition` control/capability/history/delegation records; deprecated `models`/`efforts`/`permissions` and default fields are derived by `defineProviderManifest()`. New code should use helper APIs such as `getProviderDefaultModel`, `getProviderPermissionOptions`, and `getProviderDefaultPermission` instead of reading those compatibility fields directly.
- Provider-neutral UI labels should come from `ProviderManifest.ui` and shared helpers in `src/lib/providers.ts` (`listProviderManifests`, `isProviderId`) plus `ProviderLogo`; avoid open-coded `provider === "openai"` checks for cosmetic labels, provider chips, and widget provider parsing.
- Delegation child spawning should also go through `runProviderTurn`; keep provider-specific setup limited to runtime input fields such as Claude `mcpConfig`/`noSessionPersistence` and Codex `mcpEnv`/`skipGitRepoCheck`.
- Delegation child provider setup policy lives in `ProviderManifest.delegation` (`src/lib/providers.ts`): MCP transport, OpenWolf skip policy, no-session-persistence, git-repo-check skipping, and child runtime permission metadata are provider-owned. `src/lib/delegationChildRuntime.ts` should only read that manifest policy and shape `ProviderTurnInput`.
- Delegation child history cleanup should go through provider runtimes (`deleteProviderHistory` / `provider_history_delete`) instead of callsite provider checks. Anthropic deletes the child JSONL; OpenAI intentionally returns a skipped result because blindly deleting Codex rollout files is not safe.
- Chat send validation that can return before provider IPC must run before `setStreaming(true)`, or it must explicitly clear streaming. The prompt queue depends on `isStreaming` being a real provider-turn signal, not a validation-error residue.
- Delegation MCP server logic is centralized in `mcp/delegation-common.mjs`; keep `t64-server.mjs` and `delegation-server.mjs` as thin entrypoints. The HTTP bridge helper must keep explicit `Content-Length` headers for JSON POSTs because the Rust delegation endpoints do not support chunked request bodies.
- Delegation task persistence stores child runtime facts under `DelegateTask.childRuntime` (`providerId`, model, effort, `providerPermissionId`, cwd, cleanup state). `permissionPreset` is legacy read-compat only; cleanup paths should use this metadata as the fallback when ephemeral child sessions are no longer present in `providerSessionStore`.
- Backend MCP config writers must use `Path::join` for `mcp/t64-server.mjs` and `.mcp.json` paths; frontend `joinPath` fixes are not enough because `create_mcp_config_file`, `ensure_t64_mcp`, and `ensure_codex_mcp` also write provider MCP configs.
- Delegation team-chat HTTP routes should reject oversized bodies instead of truncating at the read cap, reject malformed JSON/missing `group_id`, and bound retained/read messages so long-running delegation groups cannot grow the permission-server store without limit.
- Delegation MCP wiring differs by provider: Anthropic children need a temp MCP config path, while OpenAI/Codex children receive delegation env via runtime `mcpEnv`; future ProviderId additions must choose a transport in `getDelegationMcpTransport()` before typecheck passes.
- This repo currently has no JS test runner in package.json; provider modularity regression coverage can live as TypeScript verification fixtures under `src/` so `tsc --noEmit` typechecks them, with pure exported helpers for request shaping and routing decisions.
- Future-provider modularity coverage lives in `src/lib/providerModularity.verification.ts` as local stub types (`ProviderId | "opencode"`). Keep the fixture proving two paths: missing manifest/runtime/providerState/IPC entries fail typecheck, while a deliberately unsupported stub manifest/runtime/request/state shape compiles. With mapped provider IPC contracts, use provider-specific aliases such as `ProviderCreateRequest<"openai">` in fixtures.
- Provider conformance coverage is split by surface: frontend helper/manifest/runtime/metadata/delegation/event fixtures stay in `src/lib/providerModularity.verification.ts`, while backend adapter lifecycle/history fail-closed behavior is covered by provider registry unit tests and normalized event envelope serialization by `src-tauri/src/providers/events.rs` tests.
- Backend `ProviderAdapter` is the live common Rust contract: provider discriminator, capabilities, create/send/cancel/close, and capability-gated history methods. Do not reintroduce a separate `ProviderCommandAdapter` or async t3code-shaped stubs unless the UI/backend actually consume them; registry tests assert Claude, Codex, and Cursor implement the same contract.
- Backend provider snapshots are adapter-owned: `ProviderAdapter::snapshot()` returns the `ProviderSnapshot` for Claude/OpenAI/Cursor, and `ProviderRegistry::snapshots()` delegates to registered adapters instead of switching over central provider ids. Keep the IPC wire shape stable and provider-specific descriptor/status/capability choices in adapter-owned snapshot hooks/helpers.
- Rust provider snapshot option/control/display/default tables should live beside the owning adapter as static `SnapshotDescriptor` data, with `src-tauri/src/providers/snapshots.rs` limited to shared wire-shape construction. `src/lib/providers.ts` intentionally mirrors the same descriptor data only as offline/IPC-failure and synchronous saved-session migration fallback until provider snapshots can hydrate before store initialization.
- Rust snapshot capability fields are derived from `ProviderAdapterCapabilities`: static flags (`mcp`, `plan`, `images`, `hook_log`, `native_slash_commands`, `compact`) live on the adapter descriptor, while snapshot `fork`/`rewind` derive from `ProviderAdapterCapabilities.history`. Do not reintroduce a separate snapshot-only feature struct.
- Provider scaffold workflow lives in `scripts/scaffold-provider.mjs` plus `docs/provider-scaffold.md`: the generator creates provider-owned runtime/decoder/backend-adapter stubs and a per-provider checklist, but intentionally leaves shared registry/contract edits manual so parallel agents can coordinate `src/lib/providers.ts`, `providerRuntime.ts`, event ingestion, `claudeStore`, Rust provider registration, and verification fixtures.
- Delegation spawn request coverage is easiest to keep stable by factoring the child `ProviderTurnInput` shape into a pure helper, then verifying provider-specific fields in `src/lib/providerModularity.verification.ts` instead of mocking React hooks/timers.
- Provider lifecycle setup now has two ownership points: frontend `ProviderRuntime.prepareTurn` handles frontend-only prep such as Claude temp MCP config creation from delegation env and Codex skill bridging, while backend `ProviderAdapter::prepare_create_session` / `prepare_send_prompt` own provider-side setup. Claude registers permission/OpenWolf sidecars and ensures root `.mcp.json` there; Codex and Cursor ensure their MCP configs there. `ensure_t64_mcp` / `ensure_codex_mcp` / `ensure_cursor_mcp` IPC commands are compatibility/manual wrappers only.
- Provider permission presets are canonical in `ProviderSessionState.providerPermissions[providerId]`; OpenAI provider metadata stores thread metadata only, and `selectedCodexPermission` should appear only in legacy persisted-state read/migration shims and fixtures. New provider turn inputs should pass `providerPermissionId`, and UI controls should render from `ProviderManifest.controls` instead of assuming model + effort + permission for every provider.
- Provider topbar controls are canonical in `ProviderSessionState.selectedControls[providerId][controlId]` and `ProviderManifest.controls` is an ordered array of provider-declared control definitions. `selectedModel`/`selectedEffort` are compatibility mirrors only for controls with legacy slots; Cursor's topbar mode is `selectedControls.cursor.mode` and must not be mirrored through `selectedEffort`.
- Open provider sessions must stamp missing topbar/control defaults into per-session `selectedControls` on mount/provider change. Otherwise controls that fall back to `settingsStore.providerControlDefaults` will visually change in every open unnamed session when one topbar change updates the global default.
- Provider controls and provider snapshots use the same generic select-control descriptor shape: `id`, `label`, `kind`, `scope`, `defaultValue`, and `options`. Current scopes are `topbar` and `composer`; old `placement`/`values` names are compatibility-only and should not be used for new provider surfaces. Backend snapshots are exposed through `provider_snapshots`; frontend callers should use `src/lib/providerSnapshots.ts` so backend data is merged over manifest fallbacks.
- Provider control descriptors now support `select`, `boolean`, `text`, and `number` kinds with typed `defaultValue` plus optional typed option `value` fields. Existing providers still use string select controls; legacy model/effort/permission mirrors must narrow selected control values back to strings before use.
- Provider picker/settings UI should use `src/lib/providerSnapshots.ts` display/control/capability helpers first. Keep `settingsStore.providerAvailability` as the user-owned enabled/hidden toggle; snapshot install/status labels are display metadata, not saved-session loading gates.
- Settings provider defaults are edited from the Providers section using `listProviderSnapshotControls()` and persisted in `settingsStore.providerControlDefaults`; Claude-only settings, including OpenWolf, belong under the Anthropic/Claude provider card rather than separate global sections.
- Provider conformance coverage now has a separate frontend fixture file, `src/lib/providerConformance.verification.ts`, for snapshot fallback/wire-shape, Cursor runtime event normalization, runtime metadata, unsupported history, and delegation child runtime boundaries. Use it for cross-boundary assertions that would otherwise churn `providerModularity.verification.ts` during parallel provider work.
- Zustand selectors used by React must not return freshly-created provider-state/control objects. `resolveSessionProviderState()` constructs a new object, so selectors should return primitives or stable store-owned subobjects and derive normalized objects in `useMemo`; otherwise saved provider panels can trigger React `getSnapshot should be cached` / maximum update-depth blank roots.
- Provider semantic projection is split by ownership: `src/lib/providerEventSemantics.ts` is the shared dispatcher/state shim, while `src/lib/providerRuntimes/*EventSemantics.ts` project provider-owned MCP/file/plan/task/question/delegation text semantics. Keep the dispatcher fallback limited to provider-neutral MCP tool conventions such as `StartDelegation`.
- Canonical backend stream events now use `ProviderRuntimeEvent` envelopes with `type` categories `provider.session`, `provider.turn`, `provider.content`, `provider.tool`, `provider.mcp`, and `provider.error`. Cursor emits these on `provider-event` first, while its decoder still accepts raw Cursor `stream-json`; Claude/OpenAI legacy raw decoders remain the fallback path.
- OpenAI/Codex backend emission now routes recognized legacy/app-server events through `emit_codex_json()`: lifecycle, assistant content, tool start/update/complete, MCP status, turn completion, and errors emit `ProviderRuntimeEvent` envelopes on `provider-event`, while the old `codex-event` payload is still emitted for compatibility. Unrecognized Codex events continue to use legacy provider-event decoding as fallback.
- Rust `providers::events::experimental::ProviderEvent` is reference-only for the larger t3code event matrix and is not re-exported from `providers`; backend adapters should construct `ProviderRuntimeEvent` and emit it through `emit_provider_runtime_event()`.
- Provider-event duplicate suppression in `src/lib/providerEventIngestion.ts` must be keyed by `(provider, sessionId)`, not provider-only. Claude/OpenAI can emit shared `provider-event` and legacy events side by side while individual sessions migrate to `ProviderRuntimeEvent`; legacy fallback for other active sessions must stay live.
- ProviderRuntimeEvent ingestion must validate the embedded `event.provider` and `event.sessionId` against the outer `provider-event` envelope before decoding or marking the session as unified. Mismatches should fail closed so a bad envelope cannot leak text into another session or suppress that session's legacy fallback stream.
- Claude backend event migration is stateful: `src-tauri/src/providers/claude.rs` accumulates `content_block_start` + `input_json_delta` tool input before emitting `provider.content` message/toolCalls on `message_stop`, emits `provider.tool` completions from user `tool_result` blocks, and keeps unknown Claude stream shapes on raw `provider-event` because unified sessions suppress legacy `claude-event` ingestion.
- After parallel provider-modularity work, run one integrated verification pass from the root plus `src-tauri`: `npm run typecheck`, `npm run build`, `git diff --check`, `cargo fmt --check`, `cargo check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test providers::registry -- --nocapture`. The registry test includes an intentional `should_panic` mismatch guard, so panic text there is expected when the test result is ok.
- Cursor provider support uses `cursor-agent -p --trust --approve-mcps --output-format stream-json` and `--resume <chatId>` for follow-up turns. Headless Cursor print mode exits at the workspace-trust gate without `--trust`; Terminal 64 keeps the provider-local chat id mapped in the Rust adapter for the current app process, while Cursor history/rewind/fork remain disabled until durable Cursor session metadata is added.
- Cursor CLI MCP config is editor-compatible and project-scoped under `.cursor/mcp.json`; do not reuse Claude's root `.mcp.json` writer for Cursor. `ensure_cursor_mcp` writes the Terminal 64 MCP server there before Cursor turns, uses newline-framed JSON-RPC responses (`T64_MCP_OUTPUT_FRAMING=newline`), and must write actual `T64_DELEGATION_*` values for active child turns because Cursor Agent headless `mcp list-tools` does not expand `${env:...}` placeholders.
- `/delegate` should use the Terminal 64 MCP `StartDelegation` tool instead of asking agents to emit a handwritten `[DELEGATION_START]` block. Parent/standalone T64 MCP exposes `StartDelegation`; active child delegation MCP exposes only `send_to_team`, `read_team`, and `report_done`.
- Delegation workflow parsing/planning lives in `src/lib/delegationWorkflow.ts`: `/delegate` command parsing, StartDelegation tool detection, tagged JSON fallback, legacy `[DELEGATION_START]` compatibility, planner prompt text, manifest-owned planner permission override, child cwd resolution, and child prompt labels should stay there. Chat UI and provider-owned semantic projectors should call these helpers instead of duplicating parser logic. `StartDelegation` task objects may include optional `agentName`; that name flows into child session labels and `T64_AGENT_LABEL` for MCP team chat.
- Delegation team chat is in the Rust permission server until cleanup, so export it before calling `cleanup_delegation_group`. `performMerge()` writes a markdown log to the parent cwd (`terminal64-delegation-<created>-<group>.md`) and includes the saved path plus full log in the parent/orchestrator merge prompt.
- `delegationStore` loads `terminal64-delegations` during module import before React renders. Always normalize persisted groups/tasks before pruning or building indexes; stale localStorage rows can otherwise blank the whole frontend at startup.
- Cursor Agent CLI `-p` may not expose custom MCP tools as direct callable tools even when the MCP server is configured/approved; it can fall back to generic MCP resource helpers. The Cursor adapter explicitly runs `cursor-agent mcp enable terminal-64` before turns, but `/delegate` must still accept a tagged `<T64_START_DELEGATION>{...}</T64_START_DELEGATION>` JSON payload with the same `context` + `tasks` shape.
- Cursor Agent CLI lists and injects Terminal 64 MCP tools only when the server responds with newline-framed JSON. `content-length` output makes `cursor-agent mcp list` hang and then report `terminal-64: Error: Connection failed`, even though the server itself works under direct JSON-RPC smoke tests.
- Cursor MCP `tool_call` stream events use provider-prefixed tool names such as `terminal-64-StartDelegation` and put the real tool arguments under `tool_call.mcpToolCall.args.args`. Delegation detection must accept names ending in `StartDelegation` and the Cursor decoder must unwrap nested `args.args` into the provider tool input.
- Cursor CLI does not emit Claude/Codex-style MCP startup status events in the normal `cursor-agent -p --output-format stream-json` turn stream. The Cursor adapter should synthesize a provider-neutral `mcp_status` event after approving/writing MCP config so the existing MCP menu can show `terminal-64` as ready/error.
- MCP config writers must replace only Terminal 64-owned entries (`terminal-64` / permission `t64`) and preserve all unrelated servers. Claude generated delegation/approver configs should merge existing Claude user settings plus project `.mcp.json`; Cursor project JSON should keep sibling `mcpServers`; Codex TOML edits should use `toml_edit` so sibling `[mcp_servers.*]` tables survive. If an existing MCP JSON file is malformed, fail closed instead of treating it as `{}` and overwriting it.
- Cursor provider history is not provider-owned/durable in Terminal 64 yet, so `claudeStore` persists a bounded `localTranscript` only for Cursor and saves it on user/assistant/tool mutations. Claude/Codex should continue to hydrate from their provider history instead of duplicating full transcripts in localStorage.
- Claude rewind should mirror t3code's rollback model: keep JSONL append-only for normal non-empty rewinds, compute a `--resume-session-at` cursor for the last retained visible message, trim the UI projection, and hydrate from the active `parentUuid` chain so orphaned future turns do not reappear on refresh.
- Claude empty rewind is different from normal rollback: there is no safe `--resume-session-at` cursor. Delete/ignore JSONL that has no visible conversation, set `hasBeenStarted=false` when promptCount reaches zero, and route the next prompt through fresh create instead of `--resume`.
- Rust verification commands must run from `src-tauri` unless using `--manifest-path`; `.wolf/buglog.json` is an object with a top-level `bugs` array, not a root JSON array.
- Cargo test accepts only one test-name filter before `--`; use a broader single filter such as `cargo test providers:: -- --nocapture` instead of passing multiple module filters.
- Direct `rustfmt` on individual Rust files needs `--edition 2021`; otherwise async functions are parsed as Rust 2015. Prefer `cargo fmt`, or use `rustfmt --edition 2021 ...` for targeted checks while other agents own unrelated Rust files.
- Tauri command handlers should stay private wrappers when provider adapters need to call the same behavior. Expose separate `pub(crate) *_impl` helpers instead; making `#[tauri::command]` functions `pub(crate)` can duplicate generated `__cmd__*` macro symbols and fail `cargo check` with E0255.
- With `exactOptionalPropertyTypes`, React prop types that explicitly forward optional values should allow explicit undefined (`prop?: T | undefined`) or omit the prop at the call site; forwarding `className={maybeUndefined}` into `className?: string` fails `tsc --noEmit`.
- ClaudeChat attachment/drop cleanup: `src/hooks/useChatAttachments.ts` owns attached file state, pasted image persistence, preview cleanup, and the single shared Tauri window drag/drop listener. Keep closest-chat hit-testing and zip filtering there instead of reintroducing module-level drag/drop code in `ClaudeChat.tsx`.
- `ProviderChat`'s main session selector intentionally omits `streamingText`; helpers used in that render path should accept the selector's session view and consume streaming text through the separate `hasStreamingText` boolean subscription.
- Tauri updater wiring should keep `bundle.createUpdaterArtifacts` out of the baseline config used by push CI, because a public updater key plus no `TAURI_SIGNING_PRIVATE_KEY` makes normal `tauri build` fail. Enable updater artifacts only in the release workflow with `--config '{"bundle":{"createUpdaterArtifacts":true}}'` and provide `TAURI_SIGNING_PRIVATE_KEY`.
- Installed Tauri CLI signer uses `tauri signer generate --ci -w <path>` to generate a no-password key non-interactively; `--no-password` is not a supported flag.
- Codex plan exit is a provider payload concern, not prompt text. Build-after-plan actions must pass `codexCollaborationMode: "default"` through queued/direct provider turns; `/plan` remains `codexCollaborationMode: "plan"`.
- Provider-specific turn knobs now live under `ProviderTurnInput.providerOptions.{providerId}`. Keep the top-level turn input for common routing/session fields only; Anthropic reads `providerOptions.anthropic`, OpenAI reads `providerOptions.openai`, and Cursor reads `providerOptions.cursor`.
- Provider history inputs must stay provider-neutral. Do not add OpenAI/Codex thread fields to `ProviderHistoryTruncateInput`, `ProviderForkInput`, `ProviderHydrateInput`, or `ProviderHistoryDeleteInput`; OpenAI history runtime resolves/writes Codex thread metadata through its own store metadata helper.
- Provider runtime metadata is canonical in `ProviderSessionState.runtimeMetadata[provider]` with `historySource`, generic `resume.id`, and provider-owned `runtimePayload`. OpenAI mirrors Codex thread ids to `runtimePayload.codexThreadId` plus `resume.id`; Cursor mirrors its chat id to `runtimePayload.cursorChatId` plus `resume.id`, and persists bounded local transcripts under `runtimePayload.localTranscript`. Flat `codexThreadId` and `localTranscript` are legacy read/mirror shims only.
- Provider turn hot paths should pass `ProviderTurnInput.runtimeMetadata` plus `selectedControls`; `threadId`, `selectedModel`, and `selectedEffort` are deprecated compatibility fallbacks only. Runtime request builders should derive resume ids through `getProviderTurnResumeId()` and model/effort/mode from provider controls.
- Codex app-server resume ids are only valid while a matching rollout JSONL exists under `~/.codex/sessions`. If `find_codex_rollout(thread_id)` misses, `thread/resume` will fail with `no rollout found`; send paths should start a fresh app-server thread and history hydrate should return `codex_rollout_missing` so the frontend clears `runtimeMetadata.openai.resume.id`.
- Provider event hot paths should derive model context from provider-owned selected controls (`getProviderSelectedControlValue` + the provider's model control) instead of reading the `selectedModel` compatibility mirror; `event.threadId` should immediately hydrate `runtimeMetadata.resume.id` through `setProviderRuntimeResumeId`.
- Provider session metadata should not depend on one giant renderer `localStorage` aggregate. t3code persists provider runtime/session projections as row-level durable records keyed by thread id; Terminal 64 mirrors that in the renderer with per-session metadata row keys plus an index and treats `terminal64-claude-sessions` as compatibility-only. App/session-browser/Discord metadata reads should use the store snapshot helpers so row data is visible even when the legacy aggregate is missing or quota-pruned.
- Skill reminder injection is provider-prompt-only. Never render or persist `<system-reminder>...</system-reminder>` blocks in visible user chat; strip those blocks at provider history mapping and provider-session store boundaries because Codex/Claude history can replay the augmented prompt after reload even when live send displayed the clean prompt.
- Provider tool calls should be normalized at the frontend contract/store boundary into Claude-style names. Use `normalizeProviderToolCall` / `normalizeProviderToolPatch` in `src/contracts/providerEvents.ts`; live decoders, history hydration (`mapHistoryMessages`), and persisted local transcripts should all render names such as `Bash`, `Read`, `Edit`, `WebSearch`, and `mcp__server__tool`.
- Provider-chat tool collapse/presentation is centralized in `src/components/provider-chat/toolPresentation.ts`: use `isGroupableToolCall`, `toolHeader`, `toolGroupLabel`, and `toolGroupItem` for row grouping, permission card labels, collapsed group summaries, and expanded group metadata instead of putting tool-name switches in React components.
- Discord session tracking is frontend-driven through `rename_discord_session` / `rename_or_link_session`; sync named sessions both when provider session metadata changes and when a provider panel opens for already-existing metadata. Backend Discord routing only works for mounted panels because `discord-prompt` is consumed by the panel-local `ProviderChat` listener.
- Voice send from `ChatInput` must snapshot the live textarea before rollback/clear. `committedBaseRef` is the pre-partial base, so rolling back first can erase a one-utterance Whisper transcript and send an empty prompt.
- Discord voice-note routing must never emit `discord-prompt` with an empty `formatted_prompt`; if Whisper transcription fails/returns empty and there is no text/file fallback, notify Discord and skip the provider turn.

### Permission server bypass + unknown-session race (2026-04-22)
- `permission_server::handle_connection` must parse `permission_mode` from the hook payload BEFORE the session_map lookup. The request's `secret` in the URL has already proven authenticity, so an unknown `run_token` is just "session unregistered" (rewind cancel+close race, spawned-session timing, or server-restart leftover). On bypassPermissions, always return `permissionDecision: "allow"` even with empty session_id — otherwise the user gets silent `permissionDecision: "deny"` with reason "Unknown session — denied for safety" on skill/widget/MCP/MD edits.
- Claude CLI's hook payload fields are snake_case at runtime (`permission_mode`, `hook_event_name`, `tool_name`, `tool_input`, `session_id`). The minified binary uses `permissionMode` internally but serializes to snake_case. Verified by probing with a python hook on `claude --print --permission-mode bypassPermissions --settings <file>`.

### Claude CLI sensitive-file classifier is unbypassable in --print mode (2026-04-22)
- The classifier (RC5/jtH in the minified binary) blocks Write/Edit/MultiEdit on hardcoded paths regardless of permission mode or CLI flags. Protected dirs: `.git`, `.vscode`, `.idea`, `.claude`, `.husky` (with exceptions for `.claude/skills|agents|commands|worktrees` and `.claude/scheduled_tasks.json`). Protected filenames at basename: `.gitconfig`, `.gitmodules`, `.bashrc`, `.bash_profile`, `.zshrc`, `.zprofile`, `.profile`, `.ripgreprc`, `.mcp.json`, `.claude.json`. Both `--permission-mode bypassPermissions` and `--dangerously-skip-permissions` are ignored. The only allow-rule prefix that satisfies the early-return is `Edit(/.claude/**)` or `Edit(~/.claude/**)` ending in `/**` — so `.mcp.json` cannot be unblocked via settings.
- The classifier runs BEFORE PreToolUse hooks fire, so a hook-based auto-allow is impossible. The only workaround is to detect the sensitive-file tool_result error in the frontend and perform the edit ourselves (Terminal-64 pattern: `pendingSensitiveEdit` in claudeStore + `applySensitiveEditAndContinue` in ClaudeChat.tsx — reads file via `readFile`, replays Write/Edit/MultiEdit semantics locally, writes via `writeFile`, then injects a follow-up user message so Claude treats the tool call as succeeded).

### Claude CLI event handling (2026-04-19)
- "Safety net" sets `isStreaming=true` on any non-result/non-ping event. Top-level `{type:"error"}` (rate limit, overloaded, auth) MUST be handled explicitly: `setError` + `setStreaming(false)` + clear pending + `return`. Otherwise spinner never stops.
- `content_block_delta` + `input_json_delta`: always check `blocks[last].type === "tool_use"` before accumulating inputJson — `thinking` blocks can interleave.

### Claude CLI --resume replay (2026-04-19)
- If the previous run died mid-tool, the session JSONL can contain an assistant `tool_use` block with no matching user `tool_result`. On `--resume`, the CLI RE-EXECUTES the dangling tool (infinite replay). Before every spawn, scan the JSONL and append synthetic cancelled `tool_result` records (is_error: true) for any unresolved tool_use IDs. See `sanitize_dangling_tool_uses()` in `claude_manager.rs`. Preserve `parentUuid`, `cwd`, `version`, `gitBranch` on the synthetic record so the CLI accepts it.
- Claude CLI stdout lines can be hundreds of MB for large Bash outputs. Emitting them raw as one Tauri event freezes the renderer (JSON.parse + React render + localStorage persistence on megabytes). Cap lines > 512KB in the reader thread (`cap_event_size()`) — truncate `tool_result`/`text` content to head 96KB + tail 96KB with a marker. The CLI's own JSONL keeps the full output for future turns; only the live UI stream is truncated.
- `cap_event_size()` is shared by Claude and Codex live streams. If it receives a parsed JSON event, every fallback must preserve valid JSON; raw byte-slice truncation makes the frontend show bogus parse errors while the provider process keeps running. For provider events with arbitrary shapes, recursively truncate oversized string fields and re-serialize.
- LegendList row keys must change when a row's rendered height can change outside normal message append flow, especially assistant tool cards receiving live output. Include a lightweight layout signature for assistant/tool rows so the virtual list remeasures instead of letting growing cards overlap following rows.

## Decision Log

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->

### Backend provider command payload dispatch (2026-04-28)
Chosen: keep Tauri `provider_create`/`provider_send` as generic `(provider, req: serde_json::Value)` commands and pass a raw `ProviderCommandRequest` into the selected adapter, with a small sidecar context for cross-cutting setup. Rejected: central Claude/Codex request enums, because every future provider-specific create/send field would require editing the shared enum and all adapter match statements.

### Provider history capability boundary (2026-04-28)
Chosen: keep existing Claude JSONL and Codex rollout/app-server history implementations, but make support explicit through per-provider runtime/backend capabilities and structured `applied`/`skipped`/`unsupported` results. This lets future providers such as OpenCode register create/send support without immediately implementing history and without turning generic history IPC into provider-specific hard errors.

### Traveling comet border beam — SVG path animation (2026-04-19)
Chosen: SVG `<rect pathLength="100">` + `stroke-dasharray` + animated `stroke-dashoffset` + `feGaussianBlur`.
Rejected: (1) multiple discrete divs with staggered `animation-delay` on `offset-path` — always render as separated dots rather than a continuous beam. (2) conic-gradient + mask — distorts speed at corners on wide rectangles.

### Native widget bridge broker contract (2026-04-28)
Chosen: broker native widget requests through Rust with per-instance ids, explicit pending-response timeouts, and an SSE event channel. The main React webview remains the bridge host, so existing widget APIs can be shared once WidgetPanel's iframe handler is extracted. Iframe `postMessage` stays intact as the default compatibility transport.
- Native widget mode is wired through `WidgetPanel.tsx`: effective `native-webview` creates a Tauri child webview, injects a document-start bridge shim that intercepts `window.parent.postMessage` in top-level widget pages, sends requests to `/widgets/{id}/bridge/{instance}/request`, and receives host events over `/events`. Keep iframe mode as the default compatibility path while testing heavy widgets like `ro-sync`.
- Native child webviews do not inherit the zoomable canvas transform automatically. Sync both native bounds and native webview zoom from the canvas zoom, just like Browser panels; otherwise widgets render at the wrong visual scale inside a transformed canvas.
- Widget hot-reload polling must never recursively scan dependency/build/tool trees. `ro-sync` installed as `~/.terminal64/widgets/ro-sync` was 5.5G with 42,295 files, mostly under `daemon/target` and `tools`; `widget_file_modified` walking it every 1.5s caused the lag. Skip heavy dirs (`target`, `tools`, `node_modules`, `Packages`, etc.) so the same widget scans source/assets only.

- Widget creation instruction files live behind `write_widget_instruction_files` / `writeWidgetInstructionFiles`: call it only during fresh widget creation, before any widget files are generated. It writes identical provider-readable `CLAUDE.md` and `AGENTS.md` and refuses non-empty widget dirs so existing widgets are not modified.

### Provider modularity benchmark against t3code (2026-04-30)
Local t3code clone at `/tmp/t3code.OVUK2p/t3code` commit `3582288` shows the next modularity bar: server-owned `ServerProvider` snapshots, dynamic provider model/option descriptors, a broad canonical `ProviderRuntimeEvent` contract, `ProviderAdapterShape` lifecycle/event streams, DB-backed `ProviderSessionDirectory`, and runtime event ingestion. Terminal 64 is simpler and now has good manifest/runtime/provider-owned setup boundaries, but it remains more static: frontend `ProviderId`/manifest/runtime maps, Rust provider enum/registry, compatibility mirrors, and frontend-normalized event semantics still make provider additions more centralized than t3code.
- Latest T64 snapshot/event pass added real `provider_snapshots`, generic snapshot controls, Cursor-first `ProviderRuntimeEvent` envelopes, provider-owned `runtimeMetadata[provider]`, and conformance fixtures. The next benchmark gap is making snapshots truly adapter-owned/dynamic instead of duplicating frontend manifests in Rust, then moving Claude/OpenAI onto canonical runtime envelopes and reducing top-level `ProviderTurnInput` compatibility fields.
